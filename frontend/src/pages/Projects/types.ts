export type ProjectProps = {
    id?: number,
    project_id?: string,
    title: string,
    company: string,
    start_date: string,
    finish_date: string,
    status: string,
    revenue: string,
}